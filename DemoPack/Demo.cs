using System;
using System.Collections.Generic;
//using System.Linq;
using System.Text;
//------------------------------------------------------------------------------
using System.Windows.Forms;
using GlobalClass;
using MGOA_Pack;
//------------------------------------------------------------------------------
namespace DemoPack
{
    public class Demo
    {
        //------------------------------------------------------------------------------
        //静态定义
        /*private static Timer timer1 = new Timer();
        private static DemoPara[] ParaList;
        private static DemoWave[] WaveList;
        //------------------------------------------------------------------------------
        public static void Initialize()
        {//初始化
            //参数组列表
            ParaList = new DemoPara[ParaGroup.Items.Count];
            for(int i=0; i<ParaGroup.Items.Count; i++)
                ParaList[i] = new DemoPara(ParaGroup.Items[i]);
            //波形组列表
            WaveList = new DemoWave[WaveGroup.Items.Count];
            for (int i = 0; i < WaveGroup.Items.Count; i++)
                WaveList[i] = new DemoWave(WaveGroup.Items[i]);
            //触发时钟
            timer1.Enabled = true;
            timer1.Interval = 1;
            timer1.Tick += timer1_Tick;
        }
        //------------------------------------------------------------------------------
        private static void timer1_Tick(object sender, EventArgs e)
        {
            for (int i = 0; i < ParaList.Length; i++)
                ParaList[i].Tick();
            for (int i = 0; i < WaveList.Length; i++)
                WaveList[i].Tick();
        }*/
        //-----------------------------------------------------------------------------------------

    }
}
